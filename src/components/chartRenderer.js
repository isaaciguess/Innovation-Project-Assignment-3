//this component is used to render the chart
import React from "react";

export default class ChartRenderer extends React.Component {    
    render() {
      return (
          <h1>Chart Renderer</h1>
      );
    }
  }